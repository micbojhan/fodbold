@model Presentation.Web.ViewModels.CreateMatchViewModel

@{
    ViewBag.Title = "title";
}

<h2>title</h2>

<div>
    <div>
        @using (Html.BeginForm("CreateMatch", "MvcMatch", FormMethod.Post))
        {
            @Html.ValidationSummary(true)

            <div class="form-group">
                @Html.LabelFor(m => m.PlayerOne)
                @Html.TextBoxFor(m => m.PlayerOne, null, new { @class = "form-control", placeholder = "PlayerOne Initials" })
            </div>

            <div class="form-group">
                @Html.LabelFor(m => m.PlayerTwo)
                @Html.TextBoxFor(m => m.PlayerTwo, null, new { @class = "form-control", placeholder = "PlayerTwo Initials" })
            </div>

            <div class="form-group">
                @Html.LabelFor(m => m.PlayerThree)
                @Html.TextBoxFor(m => m.PlayerThree, null, new { @class = "form-control", placeholder = "PlayerThree Initials" })
            </div>

            <div class="form-group">
                @Html.LabelFor(m => m.PlayerFour)
                @Html.TextBoxFor(m => m.PlayerFour, null, new { @class = "form-control", placeholder = "PlayerFour Initials" })
            </div>

            <button type="submit" class="btn btn-default">Submit</button>

        }
    </div>
    <div>
        
    </div>
</div>

